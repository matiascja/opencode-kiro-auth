{
  "name": "@matiascja/opencode-kiro-auth",
  "version": "1.12.0",
  "description": "OpenCode plugin for AWS Kiro (CodeWhisperer). Fork of @zhafron/opencode-kiro-auth with an IDC ghost-account fix; provider id kept as 'kiro-auth' (upstream reverted to 'kiro' in 1.11.0). Not published to npm — install via file:// pointing at this repo's dist/index.js.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/fix-esm-imports.mjs",
    "format": "prettier --write 'src/**/*.ts'",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "prepare": "husky"
  },
  "keywords": [
    "opencode",
    "plugin",
    "kiro",
    "codewhisperer",
    "aws",
    "claude",
    "ai",
    "auth"
  ],
  "author": "tickernelz",
  "contributors": ["matiascja (fork maintainer)"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/matiascja/opencode-kiro-auth.git"
  },
  "private": true,
  "dependencies": {
    "@aws/codewhisperer-streaming-client": "^1.0.34",
    "@opencode-ai/plugin": "^1.15.11",
    "libsql": "^0.5.29",
    "proper-lockfile": "^4.1.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/proper-lockfile": "^4.1.4",
    "bun-types": "^1.3.6",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.4.2",
    "prettier-plugin-organize-imports": "^4.1.0",
    "typescript": "^5.7.3"
  },
  "opencode": {
    "type": "plugin",
    "hooks": [
      "auth",
      "event"
    ]
  },
  "files": [
    "dist",
    "package.json",
    "README.md"
  ],
  "lint-staged": {
    "*.ts": [
      "prettier --write"
    ]
  }
}
